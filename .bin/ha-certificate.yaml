apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: selfsigned
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: selfsigned-ca
spec:
  commonName: Self-Signed CA
  secretName: selfsigned-ca
  isCA: true
  issuerRef:
    name: selfsigned
    kind: Issuer
    group: cert-manager.io
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: selfsigned-ca
spec:
  ca:
    secretName: selfsigned-ca
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ldap-cert
spec:
  commonName: openldap.ha.svc
  dnsNames:
    - "openldap.ha.svc"
    - "openldap.ha.svc.cluster.local"
    - "*.openldap-headless.ha.svc.cluster.local"
  secretName: ldap-cert
  issuerRef:
    name: selfsigned-ca
    kind: Issuer
    group: cert-manager.io